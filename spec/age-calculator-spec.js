import { AgeCalculator } from './../age-calculator.js';

describe ('AgeCalculator', function() {
  let ageEarth;

  before(function () {
    ageEarth = new AgeCalculator(30);
  })

  it('should take in and return a number' function(
    expect(ageEarth).toEqual(30);
  ))

})
